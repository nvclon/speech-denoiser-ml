# plots/

This directory is used for training/evaluation visualizations and metrics outputs.

- The training pipeline writes plots and metric summaries here.
- Contents are ignored by git by default (see root .gitignore).
- Keep this folder in the repo to match the course requirements.
